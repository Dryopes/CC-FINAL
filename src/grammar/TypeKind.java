package grammar;

/** Pascal data type kind. */
public enum TypeKind {
	/** Integer base type. */
	INT,
	/** Boolean base type. */
	BOOL,
	/** Char base type. */
	CHR,
	/** Array compound type. */
	ARRAY;
}
